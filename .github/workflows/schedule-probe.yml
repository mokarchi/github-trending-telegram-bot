name: Scheduler Probe

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: Confirm scheduler
        run: echo "Scheduler fired at $(date -u +%Y-%m-%dT%H:%M:%SZ)"
